/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  eslint: {
    ignoreDuringBuilds: false,
    dirs: ['app', 'components'],
  },
}

module.exports = nextConfig